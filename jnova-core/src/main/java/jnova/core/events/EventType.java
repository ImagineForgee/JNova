package jnova.core.events;

public enum EventType {
    SERVER_START,
    SERVER_STOP,
    SERVER_ERROR,
    TCP_SESSION_OPEN,
    TCP_SESSION_CLOSE,
    TCP_SESSION_ERROR,
    TCP_MESSAGE_RECEIVED,
    CUSTOM
}

